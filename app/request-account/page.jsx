export const metadata = {
  title: 'Request an Account — Sukuu',
  description: 'Ask the Sukuu team to create an admin account for you.',
};

import RequestAccountForm from './RequestAccountForm';

export default function RequestAccountPage() {
  return <RequestAccountForm />;
}
